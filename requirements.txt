# Core (text mode: python run.py text  or  python run_jarvis.py)
requests>=2.31.0
python-dotenv>=1.0.0
pydantic>=2.5.0
typing-extensions>=4.9.0
websockets>=12.0

# Cloud SDKs are optional. llm/cloud_llm.py talks HTTP via requests.
# Uncomment if you want the official clients:
# openai>=1.12.0
# google-generativeai>=0.3.0
# anthropic>=0.18.0
# langchain>=0.1.0
# langchain-openai>=0.0.5

# Voice (Python 3.14 on Windows: no PyAudio wheel)
# pyaudio>=0.2.14
sounddevice>=0.5.0
numpy>=1.26.0
pyttsx3>=2.90
edge-tts>=7.0.0
vosk>=0.3.45
webrtcvad>=2.0.10
pocketsphinx>=5.0.0
# speechrecognition>=3.10.0  # typically needs PyAudio

# asyncio is in the stdlib — do not pip install it

# Optional extra TTS (not required for text mode)
# gTTS>=2.5.0
# pygame>=2.5.2
